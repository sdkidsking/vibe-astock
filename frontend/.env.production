# 线上挂在 https://7uffy.top/astock/ 子路径（nginx 反代剥前缀到 127.0.0.1:8910）。
# `npm run build`（production 模式）自动读取本文件；`npm run dev` 不读，仍走根路径 + /api 代理。
# 想回到独立站点根路径部署（裸 IP:8910）：删掉本文件重 build 即可。
VITE_BASE=/astock/
